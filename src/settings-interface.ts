// Settings interface
export interface ISettings {
  canvasWidth: number;
  canvasHeight: number;
  backgroundColor: string;
  boundingCircleColor: string;
  randomCircleColor: string;
  padding: number;
  minCircleSize: number;
  maxCircleSize: number;
}
